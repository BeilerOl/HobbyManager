# Tests

Cross-cutting tests: E2E, API contract, integration.
- `e2e/` – end-to-end scenarios (full stack)
- `integration/` – API contract / integration tests

Unit and integration tests for backend live in `backend/`; frontend unit tests in `frontend/`.
