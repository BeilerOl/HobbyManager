# HobbyManager Backend (Go)

REST API server. Implements the contract defined in `../api/openapi.yaml`.
Uses SQLite by default; repository layer allows swapping to PostgreSQL later.

Run: `go run ./cmd/server`
